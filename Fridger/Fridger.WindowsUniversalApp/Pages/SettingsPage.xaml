<Page
    x:Class="Fridger.WindowsUniversalApp.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fridger.WindowsUniversalApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
<!--x:Key="ButtonStyles"-->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <Style 
               TargetType="Button">
                <Setter Property="FontSize" Value="36"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="Margin" Value="10"/>
            </Style>
        </Grid.Resources>
        <StackPanel>
            <TextBlock FontSize="72" Text="Settings page" HorizontalAlignment="Center"/>
            <Button Margin="50" Content="Manage Households"/>
            <Button Margin="50" Content="Manage Fridges"/>
            <Button Margin="50" Content="Manage Pinpointed Shops"/>
            <Button Margin="50" Content="Manage Account Settings"/>
            <Button Margin="50" Content="Upgrade to Premium" 
                    Background="GreenYellow"
                    FontSize="42"/>
        </StackPanel>
    </Grid>
</Page>
